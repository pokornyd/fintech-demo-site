phoneHeight = 869px;
phoneWidth = 419px;
frameHeight = 812px;
frameWidth = 375px;
frameTopOffset = 95px;
frameLeftOffset = ((phoneWidth - frameWidth) * 0.5 - 2px);

scrollbarWidth = 17px;
body {
  margin 0
}

.phone-emulator__iframe
  border: 0
  height: 100%
  width: 100%

.config {
  position: absolute
}

.color-picker__title,
.device-picker__title {
  font-size 36px
  color: #2c2b2c
  font-family: Arial
}

.device-picker__select {
  width: 100%
  color: #2c2b2c
  border-radius: 5px
  font-size: 36px
}

.color-picker {
}

.device-wrapper {
  display flex
  justify-content center
  height 100vh
  align-items center
}
